export class ResultTest {
  correctPosition: number;
  correctColor: number;

  constructor( correctPosition: number, correctColor: number ) {
    this.correctPosition = correctPosition;
    this.correctColor = correctColor;
  }
}